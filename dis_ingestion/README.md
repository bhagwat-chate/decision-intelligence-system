# dis-ingestion
dis-ingestion is the ingestion layer of the Decision Intelligence System (DIS). It validates, normalizes, and enriches raw inputs from multiple sources into a canonical, traceable payload, ensuring input quality, schema consistency, and end-to-end traceability for downstream scoring and reasoning.
